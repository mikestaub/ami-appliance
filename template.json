{
    "variables": {
    },

    "provisioners": [
        {
            "type": "shell",
            "inline": ["sudo aptitude install -y ansible"]
        },
        {
            "type": "ansible-local",
            "playbook_file": "ansible/arangodb.yml"
        }
    ],
    "builders": [
        {
            "type": "amazon-ebs",
            "region": "eu-west-1",
            "source_ami": "ami-3907d64e",
            "instance_type": "t1.micro",
            "ssh_username": "ubuntu",
            "ami_name": "arangodb-2.2.2-{{timestamp}}"
        }
    ]
}
